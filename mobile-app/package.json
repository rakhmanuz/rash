{
  "name": "rash-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "rash.uz — Capacitor Android (production URL)",
  "scripts": {
    "cap:sync": "cap sync",
    "cap:open": "cap open android",
    "cap:run": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
